services:
  mongodb:
    image: mongo:7
    container_name: wpp_mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: wpp_laptop
    restart: unless-stopped

volumes:
  mongodb_data:
